import { AuditTab } from "@/components/AdminConsole";

export const dynamic = "force-dynamic";
export const metadata = { title: "Admin — Audit" };

export default function AdminAuditPage() {
  return <AuditTab />;
}
