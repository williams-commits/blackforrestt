import { IntegrationsTab } from "@/components/AdminConsole";

export const dynamic = "force-dynamic";
export const metadata = { title: "Admin — Integrations" };

export default function AdminIntegrationsPage() {
  return <IntegrationsTab />;
}
