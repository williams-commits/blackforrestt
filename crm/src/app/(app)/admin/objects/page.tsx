import { ObjectsTab } from "@/components/AdminConsole";

export const dynamic = "force-dynamic";
export const metadata = { title: "Admin — Objects" };

export default function AdminObjectsPage() {
  return <ObjectsTab />;
}
