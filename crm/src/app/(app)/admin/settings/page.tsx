import { SettingsTab } from "@/components/AdminConsole";

export const dynamic = "force-dynamic";
export const metadata = { title: "Admin — Settings" };

export default function AdminSettingsPage() {
  return <SettingsTab />;
}
