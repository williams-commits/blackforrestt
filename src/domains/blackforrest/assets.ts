/** Blackforrest asset registry — validated by domain:validate / doctor. */
export const BLACKFOREST_ASSETS = {
  ogImage: "/og.png",
  brandLogo: "/og.png", // generated /brand/*.svg routes exist; og is the checked file asset
} as const;
