/** Blackforrest SEO defaults (env BRAND_OVERRIDES still overrides at runtime). */
export const BLACKFOREST_SEO = {
  titleSuffix: "Black Forest Digital",
  metaDescription: "Multi-asset trading platform",
  ogImage: "/og.png",
} as const;
