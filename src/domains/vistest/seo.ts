/** SEO defaults (BRAND_OVERRIDES still overrides at runtime). */
export const VISTEST_SEO = {
  titleSuffix: "vistest",
  metaDescription: "Describe this domain in one sentence.",
  ogImage: "/brands/vistest/og.png",
} as const;
