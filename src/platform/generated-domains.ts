// Re-export of the generated domain manifest list. The indirection keeps
// next.config.ts (which compiles this chain) on RELATIVE imports only.
export { DOMAINS } from "../domains/.generated/domains";
